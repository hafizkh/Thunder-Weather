[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "build"

# Redirect for Single Page Applications (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
